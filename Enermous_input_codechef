#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int n,k;
	int c=0;
	cin>>n>>k;
	for(int i=0;i<n;i++)
	{
		long long int t;
		cin>>t;
	    if(t%k==0)
	    {
		    c++;
	    }
    }
	cout<<c<<endl;
	return 0;
}
